import { io, Socket } from "socket.io-client";
import { ClientToServerEvents, ServerToClientEvents } from "../../server/sockets";


export {
  io,
  Socket,
  ClientToServerEvents,
  ServerToClientEvents
}
